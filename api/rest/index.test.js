describe(`Test API methods`, () => {
    beforeEach(async () => {

    });

    afterEach(async () => {

    });

    it('Should be able to start service', () => {
        expect(true).toBeTruthy();
    });
});